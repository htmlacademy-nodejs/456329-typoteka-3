'use strict';

const categorieRoutes = require(`./category-routes`);
const articleRoutes = require(`./article-routes`);
const postRoutes = require(`./post-routes`);
const actionRoutes = require(`./action-routes`);

module.exports = {
  categorieRoutes,
  articleRoutes,
  postRoutes,
  actionRoutes,
};
